package model.player;

import model.Cell;
import display.State;
import display.GameDisplay;
import route.Menu;

import java.util.ArrayList;

public class RealPlayer extends Player {
    public RealPlayer(State state) {
        super(state);
    }

}
